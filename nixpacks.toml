[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["pip install --upgrade pip && pip install -r requirements.txt --no-cache-dir"]

[phases.build]
cmds = []

[start]
cmd = "/app/venv/bin/uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
