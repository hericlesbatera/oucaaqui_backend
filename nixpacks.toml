# Este arquivo é mantido apenas para compatibilidade
# O Railway deve usar o nixpacks.toml da pasta specific:
# - frontend/nixpacks.toml para o serviço frontend
# - backend/nixpacks.toml para o serviço backend
